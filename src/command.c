#include <command.h>

e_error_type_t command_factory(void *ptr, char *buffer) {
	// size_t num_of_params = cdo_get_number_of_params(command_type);
	// if (num_of_params != num) {
	// 	// This is an error to be implemented
	// 	return -1;
	// }
}

e_error_type_t command_destroy(void *command_ptr, e_command_type_t command_type) {

}